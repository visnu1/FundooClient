import { TitleInputDirective } from './title-input.directive';

describe('TitleInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleInputDirective();
    expect(directive).toBeTruthy();
  });
});
